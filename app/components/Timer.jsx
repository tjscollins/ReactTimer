var React = require('react');

var Timer = React.createClass({
  render: function() {
    return (
      <div>
        <p>Timer.jsx is Rendered</p>
      </div>
    )
  }
});

module.exports = Timer;
